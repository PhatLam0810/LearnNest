import { StyleSheet, typography } from '@styles';

const ITEM_LIBRARY = 5;
const styles = StyleSheet.create({
  container: {
    padding: 8,
    maxWidth: `${(100 - 0.5 * (ITEM_LIBRARY - 1)) / ITEM_LIBRARY}%`,
    minWidth: `${(100 - 0.5 * (ITEM_LIBRARY - 1)) / ITEM_LIBRARY}%`,
    aspectRatio: 4 / 3,
    marginBottom: 6,
    backgroundColor: 'var(--color-surface-subtle)',
  },
  image: {
    width: '100%',
    aspectRatio: 16 / 9,
    borderRadius: 8,
    objectFit: 'contain',
    overflow: 'hidden',
  },
  title: {
    ...typography.subTitle1,
    fontWeight: '500',
    marginTop: 4,
    marginBottom: 4,
  },
  desc: {
    ...typography.subTitle2,
    fontWeight: '300',
    color: 'var(--color-text-disabled)',
    marginBottom: 4,
  },
  type: {
    position: 'absolute',
    top: 12,
    left: 10,
    ...typography.body2,
    backgroundColor: 'var(--color-surface)',
    borderRadius: 8,
    paddingLeft: 8,
    paddingRight: 8,
    paddingTop: 2,
    paddingBottom: 2,
    borderWidth: 1,
    borderColor: 'var(--color-border-strong)',
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.2,
    shadowRadius: 4,
  },
  btn: {
    position: 'absolute',
    top: 20,
    zIndex: 2,
    right: 16,
    height: 24,
    ...typography.body2,
    backgroundColor: 'var(--color-surface)',
    borderRadius: 8,
    paddingLeft: 8,
    paddingRight: 8,
    paddingTop: 2,
    paddingBottom: 2,
    borderWidth: 1,
    borderColor: 'var(--color-border-strong)',
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.2,
    shadowRadius: 4,
  },
  tags: {
    ...typography.caption,
    color: 'var(--color-text-muted)',
  },
});

export default styles;
