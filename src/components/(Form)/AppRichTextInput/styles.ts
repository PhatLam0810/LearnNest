import { StyleSheet } from '@styles';

const styles = StyleSheet.create({
  inputWrap: {
    borderRadius: 8,
    borderWidth: 1,
    borderColor: 'red',
    padding: 10,
  },
});

export default styles;
